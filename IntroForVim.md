# Vim 入门

## 为什么学习 Vim

在学习 Vim 之前,我们先考虑一个看起来显然的问题:

> 什么是一个文本编辑器?

作为程序员, 我们每天其实都在和 "文本" 打交道. 源代码是文本, 配置文件是文本, Shell 脚本是文本, Markdown 文档也是文本

所谓文本编辑器 (Text Editor), 就是一个用来查看和修改纯文本文件的程序.

你大概已经使用过很多文本编辑器了. VS Code, Vim, Neovim, Notepad, 乃至很多 IDE, 其核心都包含一个文本编辑器.

**那 Vim 作为一个基于命令行的文本编辑器,它有什么优势?**

- 被广泛认可的设计哲学
    > 编程时, 你大部分时间花在阅读和编辑上, 而非书写. 正因如此,
    > Vim 是一款模态编辑器: 它针对插入文本和操作文本设有不同的模式.
    > Vim 是可编程的 (支持 Vimscript, 也支持 Python 等其他语言), 而且 Vim 的界面本身也是一种编程语言: 按键 (带有助记名称) 就是命令, 而这些命令具有可组合性.
    > Vim 避免使用鼠标, 因为鼠标操作太慢;
    > Vim 甚至避免使用方向键, 因为那也需要过多的手部移动. 最终的结果是, 这款编辑器能够跟上你思考的速度.

## 模式介绍

**普通模式 (Normal)**: 用于在文件中移动和执行编辑操作

**插入模式 (Insert)**: 用于输入文本

**替换模式 (Replace)**: 用于替换文本

**可视模式 (Visual, 含普通, 行, 块三种子模式)**: 用于选取文本块

**命令行模式 (Command-line)**: 用于运行命令

在不同的操作模式下, 同样的按键会产生不同的效果. 例如, 字母 `x`
在插入模式下只是输入字符 'x', 但在普通模式下会删除光标所在的字符, 而在可视模式下则会删除当前选中的内容.

在默认配置中,
Vim 会在左下角显示当前模式. 初始/默认模式为普通模式. 通常情况下, 你大部分时间会在普通模式和插入模式之间切换.

切换模式的方式如下: 按 `<ESC>` (退出键) 可以从任何模式返回到普通模式. 从普通模式出发, 按 `i` 进入插入模式, 按 `R`
进入替换模式, 按 `v` 进入可视模式, 按 `V` 进入可视行模式, 按 `:` 进入命令行模式.

使用 Vim 时, 你会频繁用到 `<ESC>` 键: 可以考虑将 Caps Lock 键映射为 Escape, 或者用一个简单的按键序列为 `<ESC>`
创建替代映射方案.

试试看, 在终端中打开文件:

```sh
vim example.txt
```

## 模式与基础操作

**插入文本**

在普通模式下, 按 i 进入插入模式. 此时 Vim 的行为和普通文本编辑器无异, 直到你按 `<ESC>`
返回普通模式. 掌握这一点, 再加上前面介绍的基本操作, 你就可以开始用 Vim 编辑文件了 (不过, 如果你把所有时间都花在插入模式下编辑, 效率可不算高).

**缓冲区, 标签页和窗口**

Vim 维护着一组已打开的文件, 称为 "缓冲区"
(buffers). 一个 Vim 会话 (session) 可以包含多个标签页 (tabs), 每个标签页又可以包含多个窗口 (windows, 即分割的面板). 每个窗口显示一个缓冲区. 与你熟悉的其它程序 (如网页浏览器) 不同, 缓冲区和窗口之间并非一一对应——窗口只是视图 (view). 同一个缓冲区可以在多个窗口中打开, 甚至可以在同一个标签页内做到这一点. 这非常有用, 例如, 你可以同时查看一个文件的两个不同部分.

默认情况下, Vim 启动时会有一个标签页, 其中包含一个窗口.

**命令行模式**

在普通模式下按 : 即可进入命令行模式. 按下 : 后, 光标会跳转到屏幕底部的命令行. 该模式功能丰富, 包括打开, 保存, 关闭文件, 以及退出 Vim 等.

:q – 退出 (关闭当前窗口)

:w – 保存 ( "写入" )

:wq – 保存并退出

:help {主题} – 打开帮助文档

--:help :w – 打开关于 :w 命令的帮助 --:help w – 打开关于移动命令 w 的帮助

## Vim 的界面本身就是一种编程语言

Vim 最重要的思想在于, 它的界面本身就是一种编程语言. 按键就是命令, 而这些命令可以相互组合. 这使得高效移动和编辑成为可能, 尤其是当这些命令变成肌肉记忆之后.

**移动** Vim 中的移动操作(Motion)可以理解为 "名词" , 因为它们指向文本块(Text Object).

基本移动: h (左), j (下), k (上), l (右)

单词: w (下一个词首), b (当前词首), e (当前词尾)

行: 0 (行首), ^ (行首第一个非空白字符), $ (行尾)

屏幕: H (屏幕顶部), M (屏幕中间), L (屏幕底部)

滚动: Ctrl-u (上滚), Ctrl-d (下滚)

文件: gg (文件开头), G (文件末尾)

行号: :{行号} 或 {行号}G (跳转到第 {行号} 行)

**选择 (visual mode)**

可视模式包括:

v —— 进入可视模式 (以字符为单位)

V —— 进入可视行模式

Ctrl-v —— 进入可视块模式

在可视模式下, 可以使用移动键来扩展选择范围.

**编辑操作**

编辑命令与移动命令组合使用. 这正是 Vim 界面看起来像编程语言的地方. Vim 的编辑命令(Operator)可以理解为 "动词"
, 因为动词作用于名词.

i —— 进入插入模式

但操作/删除文本时, 不应该只用退格键, 还有更好的方式

o / O —— 在当前行下方/上方插入新行

d{motion} —— 删除 {motion} 所覆盖的文本

例如: dw 删除一个单词, d$ 删除到行尾, d0 删除到行首

c{motion} —— 修改 (即删除并进入插入模式) {motion} 所覆盖的文本

例如: cw 修改一个单词, 相当于 d{motion} 后跟 i

x —— 删除光标下的字符 (等同于 dl)

可视模式 + 操作: 选中文本后, 按 d 删除或 c 修改

y —— 复制 ( "yank" , 某些命令如 d 也会复制到寄存器中) 啥是寄存器?见后文

p —— 粘贴

**计数**

你可以给名词和动词组合加上数字计数, 表示重复执行某个动作若干次.

3w —— 向前移动 3 个单词

5j —— 向下移动 5 行

7dw —— 删除 7 个单词

**个人喜欢的好用指令**

. 重复上次的修改

u 回退一次修改 Ctrl+R u 的反方向操作

J 行间拼接

I 从行首插入

m{a-z} 标记 `{a-z} 跳转到标记

Ctrl + O/I 列表间跳转

f{char} 行内找字符 t{char} 找到字符前 ; 重复 f/t

/{regex} 文件内搜索 n / N 下一个/上一个搜索结果 %s/foo/bar/g 文件内把所有 foo 换成 bar

% 匹配括号

verb + {修饰} + noun ciw 改当前 word ci" 改引号内 ca( 改括号内(包括括号本身) ct} 改直到右大括号

zz 当前行居中

gg=G 全文件重新缩进

gd 跳转到该函数/类的定义

## 创建你自己的 Vim 体验(课后作业)

1. 完成 vimtutor
2. 导入一个基础的 vimrc(https://missing.csail.mit.edu/files/vimrc)
3. (optional)适当自行设置 vim,比如说你想在 vim 里面保留 Ctrl+C 快捷键来复制到电脑 clipboard,(vim 的 yank 指令不会自动复制到电脑的 clipboard,要指定寄存器,也就是"+y,这或许不方便)
4. (optional)试着在更多地方使用 vim 的编辑模式.(下载 Vscode 的 Vim 插件, 在 shell 里面开启 Vim 模式等等)

### 本文档改编自 https://missing.csail.mit.edu/2020/editors/
